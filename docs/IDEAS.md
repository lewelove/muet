We remove any disk json generation+cleaning by rust entirely to keep connection between rust and nix purely in-memory. When evaluating `mute album manifest` generation use json generated by rust and send it to nix stdin. Nix will read it as required `--arg-from-stdin`, this way we can get rid of the `--impure` flag
